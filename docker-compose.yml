services:
  web:
    build: .
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=4321
      - POSTMARK_SERVER_TOKEN=${POSTMARK_SERVER_TOKEN}
    restart: unless-stopped
